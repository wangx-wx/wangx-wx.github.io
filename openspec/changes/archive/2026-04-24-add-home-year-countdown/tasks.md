## 1. Countdown Component

- [x] 1.1 新建首页年度倒计时组件（如 `YearCountdown`），定义展示结构：年度进度、已过时长、剩余天/时/分/秒。
- [x] 1.2 实现基于本地时间的核心计算函数：年初时间、下一年起始时间、已过比例、剩余毫秒与时间拆分。
- [x] 1.3 在组件中加入每秒更新机制（`setInterval`）并在卸载时清理，确保页面驻留时实时刷新。

## 2. Homepage Integration and UI

- [x] 2.1 在首页模板引入并渲染年度倒计时组件，放置到合适信息区块且不破坏现有布局。
- [x] 2.2 为倒计时模块补充样式（桌面/移动端），确保数字可读性和进度展示一致性。
- [x] 2.3 处理边界显示：跨年瞬间归零与新年度重算、进度值钳制到合法范围。

## 3. Verification

- [ ] 3.1 手动验证首页初次加载展示正确（已过/剩余信息齐全，格式符合预期）。
- [ ] 3.2 手动验证秒级更新与后台切回场景（数据可自动纠正，不出现长期停滞）。
- [x] 3.3 补充或更新相关测试（若项目已有前端单测/组件测试基础）以覆盖时间计算与边界逻辑。（当前仓库未提供前端单测/组件测试基座，记录为条件不触发）
